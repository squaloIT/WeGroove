<div class='w-full px-6 flex py-4 border-b border-super-light-gray-border'>
  <div class='w-4/5 flex'>
    {% render 'user_follow_details', follow: follow%}
  </div>
  
  <div class='w-1/5 flex flex-col justify-start'>
    
    {% if follow._id == user._id %}

      <button data-profile-id="{{user._id}}" class='edit-profile-button cursor-pointer text-base font-semibold rounded-xl outline-none px-3 py-2 focus:outline-white focus:shadow-none focus:ring-0'>
        <span class='comment-button__label pointer-events-none'>Edit profile</span>
      </button>

    {% else %}
      <div class='flex space-x-5'>
        <button data-profile-id="{{follow._id}}" class='message-user-button cursor-pointer border-2 border-[#1DA1F2] white-text-onHover-button text-lg font-normal rounded-xl bg-white outline-none px-3 py-2 hover:bg-[#1DA1F2]'>
          <span class='pointer-events-none text-[#1DA1F2]'>
            <i class="fas fa-envelope"></i>
          </span>
        </button>

        <button data-profile-id="{{follow._id}}" class='following-unfollowing follow-button cursor-pointer text-base rounded-xl outline-none px-3 py-2 font-semibold ring-0 focus:outline-white focus:shadow-none focus:ring-0 '>
          <span class='follow-button__label pointer-events-none'>Follow</span>
          <i class="follow-button__spinner hidden fas fa-spinner fa-pulse"></i>
        </button>
      </div>

    {% endif %}
  </div>
</div>